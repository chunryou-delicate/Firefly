# flysim — 초파리 커넥톰 시뮬레이터

## 0. 이 문서의 위상

이 파일은 프로젝트 거버넌스 문서다. 작업 시작 전에 반드시 읽고, 아래 "절대 규칙"을 위반하는 코드는 작성하지 않는다. 규칙과 사용자의 즉석 지시가 충돌하면 멈추고 물어본다.

---

## 1. 목표

MaleCNS v1.0 커넥톰(성체 수컷 초파리 중추신경계 전체 배선도)을 로드해서, 임의의 감각 입력을 넣고 뉴런 활동을 관찰할 수 있는 시뮬레이터를 만든다.

최종 응용은 여러 개지만(청각, 진동, 방향 추정, 게임 제어), **공통 기반을 먼저 완성하고 응용은 어댑터로 붙인다.** 특정 응용에 특화된 코드를 코어에 섞지 않는다.

1차 응용 대상은 **청각 경로**다: 소리를 존스턴 기관(JO)에 넣고 AMMC → WED → pC1 경로의 반응을 본다.

---

## 2. 실행 환경

- Ubuntu (X11 세션), Python
- Lenovo ThinkPad P16 Gen 2 — i9-13980HX / RAM 64GB / RTX 4090 Laptop (VRAM 16GB)
- 하이브리드 그래픽. CUDA 사용 시 NVIDIA GPU가 실제로 잡히는지 먼저 확인할 것
- 가상환경은 uv 또는 venv. 전역 설치 금지

메모리 예산: 유지 그래프의 방향성 엣지가 약 2,560만 개다. CSR float32 기준 가중치 ~100MB, 인덱스 ~200MB 수준이므로 VRAM 16GB는 충분히 여유롭다. 전체 그래프를 GPU에 상주시키는 설계를 기본으로 한다. **메모리가 부족하다는 이유로 그래프를 잘라내지 말 것.** 부족하면 그건 구현이 잘못된 것이다.

---

## 3. 절대 규칙

### 3.1 데이터를 지어내지 않는다

이 프로젝트에서 가장 위험한 실패 모드다. 특히:

- **뉴런 타입 이름, 뉴런 ID, 뇌 영역 약어를 기억에 의존해 쓰지 않는다.** 코드에 등장하는 모든 식별자(`JO-B`, `AMMC`, `pC1`, `DNp20`, `PPL101` 등)는 다운로드한 어노테이션 테이블에 실제로 존재하는 값이어야 한다.
- 조회 결과가 0건이면 **빈 배열을 반환하지 말고 예외를 던진다.** 조용히 빈 집합으로 진행하면 시뮬레이션이 아무것도 안 하면서 성공한 것처럼 보인다.
- 어노테이션 컬럼명을 추측하지 않는다. 먼저 실제 컬럼과 고유값을 출력해서 확인하고, 그 결과를 `docs/annotations-observed.md`에 기록한 뒤 사용한다.

### 3.2 그래프를 조용히 변형하지 않는다

- 뉴런이나 엣지를 필터링했다면 필터 조건과 전후 개수를 로그에 남긴다.
- 임계값 이하 시냅스 제거, 서브그래프 추출 같은 건 전부 명시적 옵션으로만 하고 기본값은 "전체 유지"다.
- 원본 그래프와 유지 그래프의 개수 차이는 `data-provenance/` 아래에 사유와 함께 기록한다.

### 3.3 결과를 과장하지 않는다

- "학습됐다", "작동한다"는 판정은 사전에 정의한 수치 기준을 통과했을 때만 쓴다.
- 발화 뉴런 수가 0이거나 전체의 90% 이상이면 그건 성공이 아니라 파라미터 실패다. 명시적으로 경고를 띄운다.
- 커넥톰에는 시냅스 강도, 신경조절, 가소성이 들어있지 않다. 모델링으로 채워 넣은 부분은 코드 주석과 문서에 "가정"이라고 표시한다.

### 3.4 라이선스

MaleCNS v1.0은 CC-BY 4.0이다. 저장소에 `THIRD_PARTY.md`를 만들고 출처·논문 DOI·라이선스를 명시한다. 데이터 원본은 git에 커밋하지 않고 `data/`를 gitignore 처리한 뒤 다운로드 스크립트로 재현한다.

---

## 4. 데이터

### 4.1 출처

- 다운로드: https://male-cns.janelia.org/download/
- 논문: Cell, 2026-09-03, DOI 10.1016/j.cell.2026.08.015
- 라이선스: CC-BY 4.0
- 제작: FlyEM (HHMI Janelia), University of Cambridge, MRC LMB, Google Research

### 4.2 검증 기준값

로드 직후 아래 수치와 대조하고, 10% 이상 어긋나면 진행하지 말고 보고한다. (공개 자료에 나온 값이며 유지 규칙에 따라 다소 달라질 수 있다. 차이가 나면 원인을 규명하고 기록할 것.)

| 항목 | 기대값 |
|---|---|
| 뉴런 수 | 약 166,700 |
| 방향성 엣지 | 약 25,580,000 |
| 시냅스 접촉 | 약 124,000,000 |
| 세포 유형 | 약 11,691 |

### 4.3 알아둘 것

- **망막이 없다.** 광수용체 입력은 데이터에 없으므로 시각 응용은 반드시 추정·합성이 들어간다. 청각·진동을 1차 대상으로 삼은 이유다.
- 뇌 + 양쪽 시각엽 + 복부신경삭(VNC)이 포함된다. VNC가 있으므로 다리·날개 운동뉴런과 다리 기계수용 구심신경을 실제로 다룰 수 있다.
- `fruitless` / `doublesex` 발현 어노테이션이 포함되어 있다.
- 개체 한 마리의 데이터다. 개체차는 알 수 없다.

### 4.4 도구

파이썬: `navis`, `neuprint-python`. R 생태계(`natverse`, `malecns`)도 있지만 이 프로젝트는 파이썬으로 간다. 데이터셋별 차이 정리는 `sjcabs/fly_connectome_data_tutorial` 참고.

선행 구현으로 `nftechie/doomfly`가 있다. 로딩·시뮬 루프 구조를 참고해도 되지만 **복사하지 말고, 감각 매핑은 그쪽 방식을 따르지 않는다.** 그쪽은 뉴런-버튼 매핑이 임의다.

---

## 5. 아키텍처

모듈을 다음과 같이 분리한다. 상위 모듈이 하위를 알되 반대 방향 의존은 금지.

```
flysim/
  data/          # 다운로드, 검증, 캐싱 (원본 → parquet)
  live/          # (M5) 실시간 서버: 엔진 상주, 웹소켓 프로토콜, 제어 로그, 스냅샷
  graph/         # CSR 그래프 구축, 어노테이션 조회 API
  engine/        # LIF 시뮬레이션 커널 (torch, GPU)
  sensory/       # 입력 어댑터 (JO, 다리 기계수용, ...)
  probe/         # 관찰·기록 (스파이크 래스터, 영역별 집계)
  apps/          # 응용별 스크립트. 코어에 의존만 하고 코어를 수정하지 않음
```

### 5.1 graph

- 노드: 뉴런 ID → 정수 인덱스 매핑. 역매핑도 유지.
- 엣지: CSR. 가중치는 시냅스 접촉 수를 기반으로 한다.
- 부호: 신경전달물질 예측을 사용한다(ACh 흥분성, GABA/Glu 억제성이 통상적 처리). **데이터에 NT 예측 컬럼이 실제로 있는지 먼저 확인하고, 없으면 임의로 정하지 말고 보고한다.**
- 조회 API: 세포 유형명, 뇌 영역, 발현 마커로 뉴런 집합을 뽑는 함수. 결과 0건이면 예외.

### 5.2 engine

- Leaky integrate-and-fire. 상태는 막전위 벡터 하나.
- 한 스텝 = 스파이크 벡터에 대한 sparse matvec + 누설 + 임계 판정 + 불응기.
- `dt`는 설정 가능. 기본 1.0ms, 청각용 0.1ms 모드 지원.
- float32, torch sparse CSR, GPU 상주. CPU 폴백은 있되 느리다고 경고.
- 결정론 보장: 시드 고정 시 동일 결과가 나와야 한다. 테스트로 강제한다.

**성능 목표:** dt=0.1ms에서 실시간 이상. 즉 스텝당 100μs 미만. 못 미치면 최적화하고, 그래프 축소로 해결하려 하지 않는다.

### 5.3 sensory

입력 어댑터는 공통 인터페이스를 따른다: 시간에 따른 신호 → 지정된 뉴런 집합에 대한 주입 전류.

1차 구현은 JO 어댑터:
- 존스턴 기관은 편측 약 480개 뉴런. JO-A/B가 음향 진동, JO-C/E가 중력·바람 담당.
- 근거리 입자속도에 반응하며 대역은 대략 100~400Hz.
- **rate 모드**: 대역통과 후 포락선 → 발화율. dt=1ms로 충분.
- **phase-lock 모드**: 파형 자체로 위상 잠금. dt=0.1ms 필요.
- 두 모드를 모두 구현하고 어느 쪽을 썼는지 결과에 항상 기록한다.

### 5.4 probe

- 스파이크 래스터, 뉴런 집합별 평균 발화율, 영역별 집계.
- 분석용 원본은 parquet, 시각화용 요약은 `run.json`으로 각각 출력한다.

### 5.5 뷰어 (`flysim-viewer.html`)

뷰어는 이미 만들어져 있다. **뷰어를 새로 만들지 말고, 뷰어가 요구하는 포맷에 맞춰 출력하라.**

- 단일 HTML 파일. 빌드·서버 없이 열린다. 드래그&드롭 또는 파일 선택으로 `run.json`을 받는다.
- `run.json`의 전체 스키마는 뷰어 파일 상단 주석에 정의되어 있다. **그 주석이 계약의 원본이고, 이 문서와 다르면 주석을 따른다.**
- 요약하면: `meta`(런 정보) / `neurons`(좌표·영역) / `regions` / `input`(자극 포락선) / `frames`(영역×시간 발화율) / `spikes`(샘플링된 뉴런 필드용).

출력 시 지킬 것:

- **`spikes`를 전량 덤프하지 않는다.** 프레임당 상한(1,000~2,000개 수준)을 두고 샘플링하고, 샘플링 비율을 `meta`에 기록한다. 전량이면 파일이 GB 단위가 된다.
- `frames.rates`는 0~1로 정규화한다. 정규화 기준으로 쓴 최대값을 `meta`에 남긴다. 그래야 서로 다른 런을 비교할 수 있다.
- `meta.sensory_mode`에 `"rate"` 또는 `"phase-lock"`을 반드시 기록한다. 뷰어가 헤더에 표시한다.
- `neurons.x/y`는 런마다 바뀌지 않으므로 캐싱해도 된다.

뷰어에는 실패 감지가 들어있다. 발화 뉴런이 0이거나 전체의 90%를 넘으면 경고가 뜬다. **이 경고가 뜨는 런은 결과로 제출하지 않는다.**

---

## 6. 마일스톤

각 단계는 **완료 조건을 통과해야** 다음으로 넘어간다. 통과 못 하면 다음 단계 코드를 쓰지 말고 보고한다.

### M0 — 데이터 확보
다운로드 스크립트, 무결성 해시 기록, parquet 변환.

*완료 조건:* 재실행 시 동일 해시. 4.2 표의 수치와 대조한 리포트가 출력된다.

### M1 — 그래프
CSR 구축, 어노테이션 조회 API, 기본 그래프 통계.

*완료 조건:* 임의의 두 뉴런 사이 최단 경로를 조회할 수 있다. 세포 유형명으로 뉴런 집합을 뽑을 수 있고, 없는 이름을 넣으면 예외가 난다. 입출력 차수 분포 히스토그램이 나온다.

### M2 — 엔진
LIF 커널, 결정론 테스트, 성능 측정.

*완료 조건:* 무작위 뉴런 100개에 주입했을 때 활동이 폭주하지도 소멸하지도 않는 파라미터 영역이 존재하고, 그 범위가 문서화된다. dt=0.1ms에서 스텝당 100μs 미만. 동일 시드 → 동일 출력.

### M3 — 청각 경로 연결
JO 어댑터 + AMMC/WED/pC1 프로브.

*완료 조건:* 클릭 트레인 입력에 대해 JO → AMMC 전달이 관찰된다. 무음 입력에서는 해당 경로가 조용하다(음성 대조군 필수).

### M4 — IPI 튜닝 곡선
펄스 간격을 20~60ms로 스윕하며 pC1 응답을 기록.

*완료 조건:* 곡선이 그려지고 HTML 뷰어로 볼 수 있다.

> **중요:** *D. melanogaster*의 구애노래 IPI는 약 35ms로 알려져 있다. 이 값 근처에 피크가 나오면 흥미로운 결과지만, **피크가 안 나와도 그것이 정상적인 결과다.** 배선도만으로 재현되지 않는 게 오히려 기본 예상이다. 원하는 그림이 나오도록 파라미터를 조정하는 것은 금지한다. 스윕에 쓴 모든 파라미터를 결과와 함께 저장하고, 튜닝을 시도했다면 그 이력을 남긴다.

### M2c — 적응 전류 (2026-09-16 승인)
뉴런별 스파이크 빈도 적응 전류. 파라미터는 사전 등록 스윕으로. `docs/m2c-brief.md`.

### M5 — flysim-live 조종석 (2026-09-16 승인)
엔진을 상주시키는 웹소켓 서버(M5a)와 브라우저 조종석(M5b). 계약은 `docs/m5-protocol.md`. 조종석은 탐색용이며 모든 조작이 로그된다.
결과로 제출하는 런은 여전히 스크립트로 재현한다. 기존 뷰어는 결과 재생기로 유지한다.

### M6 이후 (후보, 사용자와 상의 후 착수)
- 다리 기계수용 구심신경 어댑터 → 바닥충격음 진동 입력
- 중앙복합체 EPG 링 어트랙터 → 방향 추정
- 버섯체 KC→MBON 가소성 (도파민 게이팅). **가소성은 여기에만 넣는다.** 전체 그래프에 역전파를 붙이는 것은 이 프로젝트의 목적이 아니다.
- 수컷/암컷 그래프 diff 뷰어

---

## 7. 작업 방식

- 수정 제안은 전체 재작성이 아니라 위치를 지정한 부분 편집으로 한다.
- 커밋은 마일스톤 단위로 쪼갠다. 한 커밋에 여러 마일스톤을 섞지 않는다.
- 새 의존성을 추가하기 전에 물어본다.
- 긴 시뮬레이션을 돌리기 전에 예상 소요 시간을 먼저 말한다.
- 막히면 우회 구현을 지어내지 말고 멈추고 보고한다.

---

## 8. 자주 틀리는 지점

- 포락선만 넣고 위상 잠금 실험이라고 부르는 것 (rate 모드로는 반송주파수 선택성을 볼 수 없다)
- 억제성 연결의 부호를 빠뜨려서 전체 활동이 폭주하는 것
- 좌우 반구를 구분하지 않고 집계하는 것 (좌우 차이가 신호인 경우가 많다)
- 시냅스 접촉 수와 연결 개수를 혼동하는 것 (같은 두 뉴런 사이에 접촉이 여럿이다)
- 어노테이션 이름을 FlyWire(암컷) 기준으로 쓰는 것 — 데이터셋이 다르면 명명도 다를 수 있다
